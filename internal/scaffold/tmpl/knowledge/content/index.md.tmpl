# {{.AgentName}} knowledge base

Reference content for {{.AgentName}} and its domain. Everything in this
directory is image-baked and seeds {data}/workspace/docs in the container
on every boot — files are replaced wholesale, so keep agent-authored
notes in the workspace journal, not here.
