# markdownlint-cli2 config: long lines are normal here (tables, URLs,
# shell examples); MD024 allows sibling documents to repeat headings.
config:
  MD013:
    line_length: 400
  MD024:
    siblings_only: true
