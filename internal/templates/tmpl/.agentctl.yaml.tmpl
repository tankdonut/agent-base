# agentctl config for {{.ProjectName}}. Every line below is commented
# out: agentctl built-in defaults apply until you edit this file.

# engine: auto — podman when available, docker otherwise (or pin: podman | docker)
#engine: auto

# gateway_port: host port the gateway URL uses (keep it distinct per
# agent when several agents share one host)
#gateway_port: {{.GatewayPort}}
