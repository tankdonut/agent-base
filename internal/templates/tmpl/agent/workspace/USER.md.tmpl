# USER: owner facts

The agent keeps what it learns about its owner here. Replace the
placeholders during onboarding; the agent appends as it learns.

- Name: `<owner name>`
- Timezone: `<IANA zone, e.g. Europe/Berlin>`
- Preferences: `<how the owner wants reports and pings>`
- Standing context: `<facts that shape every answer>`
