// Fixture canary for the trigger-script surface. The smoke shim never
// evaluates script content — but the seeded argv must carry this file's
// path, and the loader must embed its (trimmed) bytes for currency.
json({ fire: false })
