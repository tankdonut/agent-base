# Cron job prompt specs (tests/fixtures/**/automations/) are machine-parsed —
# never reformatted by linters.
ignores:
  - "tests/fixtures/**"
config:
  MD013: false  # line length off
  MD024:
    siblings_only: true
  MD041: false
