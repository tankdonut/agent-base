# Cron job prompt specs (fixtures/**/automations/) are machine-parsed —
# never reformatted by linters.
ignores:
  - "fixtures/**"
config:
  MD013: false  # line length off
  MD024:
    siblings_only: true
  MD041: false
