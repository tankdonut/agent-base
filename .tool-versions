# asdf pins — latest versions installed locally.
#
# python: local dev on the newest CPython; the 3.11 image floor is
# enforced by the CI matrix (.github/workflows/ci.yml) and the bookworm
# image (container/Dockerfile), not by the local pin.
# Lint tools mirror the pre-commit revs in .pre-commit-config.yaml so
# direct CLI runs match ./make.sh lint. ruff stays unpinned here: it runs
# only inside pre-commit's managed env (rev v0.15.21).
# golang: agentctl toolchain (go.mod at repo root); the go directive in
# go.mod sets the minimum, this pin tracks the local toolchain.
python 3.14.7
golang 1.26.5
pre-commit 4.6.2
hadolint 2.15.1
markdownlint-cli2 0.23.2
